{
  "name": "denoiser-package-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-end test of the rewritten WebGPU/ONNX `denoiser` package.",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview --port 5174"
  },
  "dependencies": {
    "denoiser": "workspace:*"
  },
  "devDependencies": {
    "onnxruntime-web": "^1.27.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.4"
  }
}
