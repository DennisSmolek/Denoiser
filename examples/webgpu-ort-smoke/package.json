{
  "name": "webgpu-ort-smoke",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal WebGPU smoke test: run a converted OIDN ONNX U-Net through onnxruntime-web (WebGPU EP) with zero-copy GPU-buffer IO.",
  "scripts": {
    "dev": "vite --port 5176",
    "build": "vite build",
    "preview": "vite preview --port 5176"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "vite": "^5.4.0"
  }
}
